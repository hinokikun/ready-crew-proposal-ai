# SCR-130 Proposal History

## Purpose

過去の提案、出力形式、生成時間、再開導線を確認する。

## Current Implementation

CreationHistoryPanelと`/api/logs/creation-history`、CSV出力が存在する。Beautiful.aiリンク再表示も一部あり。

## Future

ProposalVersion、ExportArtifact、QualityReport、GenerationJobを紐づけ、再出力とStudio再開を安全に行う。

